//Define las rutas de conexión con la ApiRest del Backend

export const API_URL = "http://localhost:8085";

export const BaseUrl = {
  auth: `${API_URL}/auth`,
  comandante: `${API_URL}/comandante`,
  navegante: `${API_URL}/navegante`,
  nave: `${API_URL}/nave`,
  proyecto: `${API_URL}/proyectos`,
};
